; Disable functions that can execute operating-system commands.
disable_functions = exec,shell_exec,system,passthru,proc_open,popen

; Do not disclose the PHP version in HTTP response headers.
expose_php = Off
